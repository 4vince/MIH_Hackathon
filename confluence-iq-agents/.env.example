# LLM provider: "openai" | "anthropic" | "ollama"
LLM_PROVIDER=openai
# API key for the chosen provider
LLM_API_KEY=sk-...
# Optional: model override (default is per-provider in config.py)
LLM_MODEL=
